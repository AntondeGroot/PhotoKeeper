{
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.8.1"
  },
  "lint-staged": {
    "frontend/**/*.{ts,html,scss,css,json,js,md}": "prettier --write --ignore-unknown"
  }
}